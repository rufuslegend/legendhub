<!doctype html>
<html lang="en">
    <head>
        <%-include("../shared/meta")-%>
        <link href="https://www.legendhub.org<%=vm.urls.canonical%>" rel="canonical">
        <meta name="robots" content="noindex">
    </head>
    <body>
        <%-include("../shared/header")-%>
        <div class="container-fluid">
            <div class="px-0">
                <div class="row">
                    <div id="quests-categories" class="categoryListBackground" data-category-list>
                      <nav class="categoryListContainer" aria-label="Quest areas">
                        <button class="categoryListClose" type="button" data-category-list-close aria-label="Close areas"><i class="fas fa-times" aria-hidden="true"></i></button>
                        <h2>Areas</h2>
                        <ul class="categoryList">
                            <%for (let i = 0; i < vm.categories.length; ++i) {%>
                            <li> 
                                <a<%-(vm.categoryId == vm.categories[i].id && !vm.subcategoryId) ? ' class="active"' : ''%> href="<%=vm.urls.category(vm.categories[i].id)%>"><%=vm.categories[i].name%></a>
                                <%if(vm.categoryId == vm.categories[i].id) {%>
                                <ul>
                                    <%for (let j = 0; j < vm.categories[i].subcategories.length; ++j) {%>
                                    <li>
                                        <a<%-(vm.subcategoryId == vm.categories[i].subcategories[j].id) ? ' class="active"':''%> href="<%=vm.urls.category(vm.categories[i].id, vm.categories[i].subcategories[j].id)%>"><%=vm.categories[i].subcategories[j].name%></a>
                                    </li>
                                    <%}%>
                                </ul>
                                <%}%>
                            </li>
                            <%}%>
                        </ul>
                      </nav>
                    </div>
                    <div class="col">
                        <div class="row text-center">
                            <h1 class="col-12">Quests</h1>
                        </div>
                        <div class="px-0 px-md-5">
                            <form action="<%=vm.urls.form%>">
                                <%if (vm.categoryId) {%>
                                <input type="hidden" name="eraId" value="<%=vm.categoryId%>">
                                <%}%>
                                <%if (vm.subcategoryId) {%>
                                <input type="hidden" name="areaId" value="<%=vm.subcategoryId%>">
                                <%}%>
                                <div class="row">
                                    <div class="input-group col-12">
                                        <div class="input-group-prepend d-block d-md-none">
                                            <button class="btn btn-outline-primary" type="button" data-category-list-open aria-controls="quests-categories" aria-expanded="false" aria-label="Open areas">&nbsp;<i class="fas fa-caret-right" aria-hidden="true"></i>&nbsp;
                                        </div>
                                        <%if (vm.activeCategory) {%>
                                        <div class="input-group-prepend">
                                            <a class="btn btn-outline-primary" role="button" href="<%=vm.urls.search%>"><%=vm.activeCategory%>&nbsp;&nbsp;<i class="fas fa-times"></i></a>
                                        </div>
                                        <%}%>
                                        <input type="text" name="search" value="<%=vm.query.search ?? ""%>" class="form-control" placeholder="Search by title, whois, area, or content..." autofocus>
                                        <div class="input-group-append">
                                            <label for="statCheckbox" class="btn btn-outline-primary custom-control custom-checkbox mb-0" style="padding-left:2rem">
                                                <input type="checkbox" name="stat" value="true"<%-(vm.query.stat?" checked" : "")%> class="custom-control-input" id="statCheckbox">
                                                <label for="statCheckbox" class="custom-control-label" style="cursor:pointer">Stat</label>
                                            </label>
                                        </div>
                                        <div class="input-group-append">
                                            <button class="btn btn-primary" type="submit"><span class="d-none d-md-inline-block">Search</span><i class="d-inline-block d-md-none fas fa-search"></i></button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                        <br />
                        <%-include("../shared/pagination", {pageUrl: vm.urls.page, page: vm.page, rows: vm.rows, resultLength: vm.results.length, moreResults: vm.moreResults})-%>
                        <div class="row">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-header">
                                        <strong class="float-left"><%-(vm.noSearch ? "Recently Modified":"Search Results")%></strong>
                                        <div class="float-right">
                                            <a href="<%-(locals.user ? "":"/login.html?returnUrl=")%>/quests/add.html" aria-label="Add quest">
                                                <i class="fas fa-plus" aria-hidden="true"></i>
                                            </a>
                                        </div>
                                    </div>
                                    <div class="table-responsive">
                                        <table class="table table-sm table-md table-hover table-striped table-bordered mb-0">
                                            <thead class="thead-dark">
                                                <tr>
                                                    <% let sortClass = function(sortBy) {
                 if (vm.query.sortBy === sortBy)
                 return vm.query.sortAsc ? "fas fa-sort-up" : "fas fa-sort-down";
                 else if (!vm.query.sortBy)
                 return "fas fa-sort";
                 else
                 return "";
                 };%>
                 <th scope="col" class="text-center" nowrap><a class="text-reset" href="<%=vm.urls.sort("title", vm.query.sortBy === "title" ? !vm.query.sortAsc : true)%>">Title&nbsp;<i class="<%=sortClass('title')%>" aria-hidden="true"></i></a></th>
                 <th scope="col" class="text-center" nowrap><a class="text-reset" href="<%=vm.urls.sort("areaName", vm.query.sortBy === "areaName" ? !vm.query.sortAsc : true)%>">Area&nbsp;<i class="<%=sortClass('areaName')%>" aria-hidden="true"></i></a></th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <%for (let i = 0; i < vm.results.length; ++i) {%>
                                                <tr>
                                                    <th scope="row" class="font-weight-bold"><a class="text-primary" href="/quests/details.html?id=<%-vm.results[i].id%>"><%=vm.results[i].title%></a><a class="float-right" href="/quests/details.html?id=<%-vm.results[i].id%>" target="_blank" rel="noopener" aria-label="Open details for <%=vm.results[i].title%> in a new tab"><i class="fas fa-external-link-alt fa-lg" aria-hidden="true"></i></a></th>
                                                    <td>
                                                        <%_if(vm.results[i].eraName) {%><span class="text-info">(<%=vm.results[i].eraName%>)</span><%}_%>
                                                        <%=vm.results[i].areaName_%>
                                                    </td>
                                                </tr>
                                                <%}%>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <br>
                        <%-include("../shared/pagination", {pageUrl: vm.urls.page, page: vm.page, rows: vm.rows, resultLength: vm.results.length, moreResults: vm.moreResults})-%>
                        <br><br>
                    </div>
                </div>
            </div>
        </div>
        <%-include("../shared/footer")-%>
        <%-include("../shared/scripts")-%>
    </body>
</html>
